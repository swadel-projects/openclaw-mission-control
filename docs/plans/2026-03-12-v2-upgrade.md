# Plan: Upgrade to v2.0.0 (merge upstream/main)

**Date:** 2026-03-12
**Status:** ✅ Complete

## Summary

Merged 90 upstream commits (v1.3.0 → v2.0.0, commit `eaf0bb1`) into Scott's fork while preserving all local customizations.

## Conflicts resolved

| File | Resolution |
|---|---|
| `src/app/api/spawn/route.ts` | Kept upstream's `scanForInjection` + `logAuditEvent`; added back `readConfiguredModels` import and `?action=models` GET handler |
| `src/app/api/chat/messages/route.ts` | Took upstream's `const idempotencyKey` (simplified gateway path with `callOpenClawGateway`) |
| `src/components/panels/agent-spawn-panel.tsx` | Kept HEAD (our model-dropdown version); upstream had deleted it — re-wired import + `case 'spawn'` in `page.tsx` |

## Files modified post-merge (beyond auto-merge)

- `src/app/api/spawn/route.ts` — merged imports
- `src/app/api/chat/messages/route.ts` — resolved conflict, took upstream path
- `src/components/panels/agent-spawn-panel.tsx` — kept HEAD, staged
- `src/app/[[...panel]]/page.tsx` — re-added `AgentSpawnPanel` import + case

## New v2.0.0 features included (upstream)

- Memory graph panel (Reagraph)
- Onboarding wizard
- Security audit / scan panels
- Exec approval overlay
- Channels, Nodes, Skills, Debug panels
- OpenClaw doctor / update / version API endpoints
- Gateway health history + timeline
- Multi-gateway support
- Multi-tenant workspace support
- Hermes memory/tasks integration
- Google Auth + trusted reverse proxy auth
- Injection guard for spawn and chat messages
- Audit logging for spawns

## New dependencies (from v2.0.0)

- `reagraph` 4.30.8 — memory graph visualization
- `@radix-ui/react-slot` 1.2.4 — UI primitives
- `class-variance-authority` 0.7.1 — CVA for Tailwind variants

## Validation results

- ✅ `pnpm typecheck` — passes
- ✅ `pnpm lint` — passes (5 upstream warnings only)
- ✅ `pnpm build` — exit 0, 101 routes generated

## Progress log

- 2026-03-12: Completed full merge to eaf0bb1, all phases done.
